# Raw CSV copy

Convenience dump of the DeepChem MoleculeNet PPB table.

The manuscript uses the DeepChem loader `load_ppb` with z-score normalization fit on the training fold. Prefer `scripts/download_datasets.py --datasets ppb` when reproducing the paper numbers.

Featurized caches belong in `data/featurized/` and are gitignored.
